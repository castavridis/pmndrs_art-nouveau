# nav assets

Expected files (see CLAUDE.md):

| File | Status |
|---|---|
| `left.glb`, `right.glb`, `petals.glb` | **missing** – export from DCC, then `pnpm gltfjsx` |
| `env.hdr` | missing – `Lightformer`s used as fallback |
| `logo.svg` | placeholder (approximation of the pmndrs mark) – replace with the real one |
| `pill.png`, `left.webp`, `right.webp` | missing – `left.svg` / `right.svg` are procedural placeholders used by `Nav2D` |
