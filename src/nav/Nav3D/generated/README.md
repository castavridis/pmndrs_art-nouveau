Generated by `pnpm gltfjsx` (gltfjsx 6.5.3). Do not hand-edit; re-run when assets change.

The `*.tsx` components are kept as the reference for node/material names but are excluded
from the TypeScript build (gltfjsx 6.5 emits React 18 era typings). The app loads the
`*-transformed.glb` files directly via `Nav3D/assets.ts`, which normalises the exported
absolute coordinates into per-asset origins. Textures are resized to 64px because the
baked DCC materials are replaced by the shared glass material.
