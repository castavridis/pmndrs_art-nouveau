import { createContext } from 'react'

/** Per-canvas switch for the rect lights' emissive strips (NavCanvas `strips`); the tuning toggle is global. */
export const StripsContext = createContext(true)
